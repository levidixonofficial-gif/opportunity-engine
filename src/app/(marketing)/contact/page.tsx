import { LegalShell } from "../_Legal";

export const metadata = { title: "Contact" };

export default function ContactPage() {
  return (
    <LegalShell title="Contact">
      <p>
        A contact form wired to Resend ships in Phase 7. Until then, support routing and an address
        will be published here.
      </p>
    </LegalShell>
  );
}
