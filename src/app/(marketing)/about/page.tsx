import { LegalShell } from "../_Legal";

export const metadata = { title: "About" };

export default function AboutPage() {
  return (
    <LegalShell title="About">
      <p>
        Opportunity Engine is an “online income operating system”: a place to go from wanting to earn
        online to knowing what to do next, doing it, and tracking whether it works.
      </p>
      <p>
        It focuses on legitimate business models — freelancing, local services, digital products,
        content, e-commerce, AI-assisted services, and more — and on execution rather than hype.
      </p>
    </LegalShell>
  );
}
